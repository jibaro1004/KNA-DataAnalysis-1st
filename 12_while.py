# while count <= 3:
#     print(count)
#     # whlle문은 조건이 거짓이 되는 플래그를 꼭 세워야 함
# 무한루프의 강제 종료: Ctrl + C

# While문 사용 체크리스트
# 1. 반복 전 변수(시작값) 존재 여부
# 2. 반복을 하다가 언젠가 False가 될 수 있는 종료 조건 포함 여부
# 3. 변수가 거짓 방향으로 값이 변경되는지

count = 1  # 1번

while count <= 3:  # 2번
    # count = 0 # 반복문 안에 count 변수를 계속 0으로 재할당
    print(count)
    count += 1  # 3번

# ========================================================
print("======= 실습 1. while로 목표값 도달까지 반복하기 =========")
goal = 7
user_input = 0

while user_input != goal:
    user_input = int(input("숫자를 넣어보세요: "))
print("정답입니다!")

# break
# 반복을 그만 두고 싶을 때
# 예 1) [1, 1, 3, 3, 2, 1, 1, 1]
# 위 리스트를 돌면서 10 이상이 되면 중단하고 싶을 때
# 예 2) 사용자 입력값을 누적하다가 누적값이 총 15를 넘으면
# 종료하고 싶을 때
# break 사용시 즉시 for 문을 나감

input_sum = 0

while True:  # 조건만 보면 무한반복하는 코드
    input("값을 입력하세요. 값의 누적이 15를 넘으면 종료합니다: ")
    input_sum += user_input  # 누적값 업데이트

    if input_sum > 15:  # 종료 트리거
        print("누적 합계:", input_sum, "입력을 종료합니다.")
        break  # 누적 합계가 15를 넘으면 반복 종료
print("break를 통해 while문을 나가면 이후 코드가 실행됨")

# 사용자 입력값을 확인만 하고 저장할 필요가 없는 경우
while True:
    x = input("입력 (종료는 q를 입력하세요): ")
    if x == "q":
        break
    print("입력받은 값:", x)

# ===================================================================
n = int(input("횟수: "))

for i in range(n):
    v = int(input("측정값: "))

    if v > 80:
        print("이상 발생")
        print("가동 횟수:", n)
        break
    else:
        print("정상 상태")


# 실습 up down 게임
# 1~50 중 하나의 숫자를 정답으로 저장
# 사용자의 입력값 기준으로 정답이 up인지 down인지 출력
# 정답이 나오면 정답이고, 게임이 종료되었다고 출력
# ===================================================================

answer = 19
while True:
    guess = int(input("질문: 1~50 사이의 정답을 맞춰보세요."))
    if guess > 19:
        print("DOWN!")
    elif guess < 19:
        print("UP!")
    else:
        print("정답입니다!")
        break

# ==================================================================

# 최댓값 찾기
print("===== 최댓값 찾기 =====")
first = int(input("1번째 입력값: "))

# 첫 번째 입력값은 자동으로 최댓값이 됨 (비교할 다른 값이 없기 때문)
# for문을 사용해서 사용자 입력을 4번 받고
# 입력 받은 값 중에서 가장 큰 값을 출력
max_value = first
for i in range(4):
    v = int(
        input(f"{i+1}번째 입력: ")
    )  # 위에서 1번째 입력을 받고, i는 0부터 시작하기 때문에 2를 더해서 출력한다.

    # max_value에는 현 시점 최댓값
    # v에는 방금 사용자가 입력한 값
    # max_value와 v의 값을 비교해 더 큰 값을 max_value에 재할당

    if v > max_value:
        max_value = v
print("최댓값:", max_value)  # for 반복문 종료 후 최종 최댓값 출력

# =================================================================
# 흐름 표를 보고 코드 작성

total = 0

for i in [4, 7, 6]:
    if i > 5:
        total += i
print("합계:", total)

# ================================================================
print("====== 실습 2. 플래그로 조건 만족 값 검색하기 ======")
found = False
count = int(input("사용자님, 횟수를 입력하세요!"))
for i in range(count):
    value = int(input("측정값을 입력하세요!"))

    if value > 80:
        found = True
        break
if found:
    print("발견!")
else:
    print("없음!")

# ================================================================
print("======= 실습 1. 조건에 맞는 값만 출력하기 ======")
temp = [32, 28, 36, 29, 30, 35]
for i in range[temp]:
    if t >= 30:
        print("고온: ", t)

# ================================================================
print("====== 실습 3. 조건에 맞는 값만 골라 평균 구하기 ======")
temp = [32, 36, 31, 40, 26, 28, 30]
total = 0
count = 0
for t in range[temps]:
    if t > 30:
        total += t
        count += 1
print("고온 평균:", " total / count ")


# ================================================================

# 복습) sort (): 원본 배열을 오름차순으로 정렬해줌
# 하지만 반환해주지 않기 때문에 print로 바로 찍으면 None 출력
print(low.sort())

# 정렬된 배열을 출력하고 싶다면 아래처럼
low.sort()
print(low)

# ================================================================
print(" ========== 실습 4. 조건에 맞는 값으로 새 리스트 만들기 =============")
temp_list = [25, 26, 28, 30, 35, 33, 34]
hot = []
for t in range[temp_list]:
    if t > 30:
        hot.append(t)
print(temp_list)
print(len(temp_list))

# =================================================================
print("========== 실습 5. 값을 가공해 새 리스트 만들기 ====================")
celcius = [26, 28, 29, 32, 35, 36, 37]
empty = []
for c in empty:
    empty.append(celcius * 1.8 + 32)
print(empty)

# 리스트 안의 리스트
rows = [["펌프", 25], ["모터", 32], ["압축기, 28"]]
# 표(행, 열) 처럼 한 줄에 여러 값이 묶인 데이터
# 바깥 대괄호를 "행", 안쪽 인덱스 리스트를 "열"

print(rows[0])  # ['펌프', 25]
print(type)(rows[0])  # <class 'list'>
print(type(rows))  # <class 'list'>
# 중첩된 리스트 안의 값에 접근
print(rows[1])
# 1. rows[1]을 찾음 -> ["모터", 32]
# 2. print(["모터", 32][1]) -> [1] 앞의 리스트에서 1번 인덱스 값에 접근
# 3. print(32)
# 중첩된 리스트 내부의 값은 대괄호를 여러번 이어서 접근

# 리스트 안의 리스트 온도값만 출력하기
for row in rows:
    print(row[0], "온도", row[1])  # 펌프 온도 25 ...
# rows는 리스트를 담고 있는 큰 리스트
# row는 rows 안에 있는 작은 리스트 예) ["펌프", 25] 하나

# ==========================================================
print("===== 실습 6. 센서 데이터 종합 분석하기 =====")
temps = [32, 35, 36, 37, 38, 39, 33, 31, 40, 38]
total = 0
for t in temps:
    total += t
print("전체 평균:", total / len(count))
hot = []
for t in temps:
    if t > 30:
        hot.append(t)
hot_total = 0
for h in hot:
    hot_total += h
print("고온 개수:", len(hot))

# ============================================================
